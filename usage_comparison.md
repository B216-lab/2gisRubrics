# СРАВНЕНИЕ СПОСОБОВ ИСПОЛЬЗОВАНИЯ

## 🎯 В ОДНОЙ ТАБЛИЦЕ

| Параметр | quick_run.py | interactive_menu.py | В коде как модуль |
|----------|-------------|-------------------|------------------|
| **Установка** | 1 минута | 1 минута | 2 минуты |
| **Для новичка** | ✅ Проще | ✅ Интуитивнее | ❌ Нужен Python |
| **Разные файлы** | ❌ Жёсткие имена | ✅ Любые пути | ✅ Полный контроль |
| **Ручная проверка** | ❌ Только экспорт | ✅ Онлайн-просмотр | ✅ Полная гибкость |
| **Batch-обработка** | ✅ Хорошо | ✅✅ Отлично | ✅✅✅ Идеально |
| **Автоматизация** | ✅ Можно в батнике | ✅ Сложновато | ✅✅✅ Просто |
| **Скорость** | ⚡ Мгновенно | 2 сек загрузки | Как нужно |
| **Встроенная статистика** | ❌ Нет | ✅ Да | ❌ Нужна своя |

---

## 📊 СЦЕНАРИИ ИСПОЛЬЗОВАНИЯ

### Сценарий 1: "Нужно один раз классифицировать файл"

**Решение:** `quick_run.py` (15 секунд)

```bash
# 1. Подготовка (1 раз)
pip install sentence-transformers scikit-learn pandas numpy

# 2. Кладёшь категории в categories.csv
# 3. Кладёшь рубрики в rubrics.txt
# 4. Запускаешь
python quick_run.py

# 5. Берёшь results.csv
```

**Плюсы:**
- Не нужно ничего настраивать
- Всё просто и быстро
- Одна команда

**Минусы:**
- Жёсткие имена файлов (categories.csv, rubrics.txt)
- Нет промежуточного просмотра

---

### Сценарий 2: "Обрабатываю много разных файлов"

**Решение:** `interactive_menu.py`

```bash
# 1. Запускаешь один раз
python interactive_menu.py

# 2. Загружаешь категории (один раз)
# 3. Загружаешь rubrics1.txt, смотришь результаты
# 4. Загружаешь rubrics2.txt, смотришь результаты
# 5. Загружаешь rubrics3.csv, смотришь результаты
# 6. Экспортируешь в разные форматы
```

**Плюсы:**
- Легко переходить между файлами
- Встроенный просмотр и статистика
- Можно менять параметры (top_n)
- Удобное меню

**Минусы:**
- Медленнее для массового использования
- Требует ввода команд

---

### Сценарий 3: "Интегрирую в свой скрипт"

**Решение:** Использовать как модуль

```python
from rubrics_classifier import RubricsClassifier
import pandas as pd

# Загружаю категории
categories_df = pd.read_csv('my_categories.csv')
categories = [...]

# Инициализирую классификатор
classifier = RubricsClassifier()
classifier.load_categories(categories)

# Классифицирую свои рубрики
my_rubrics = ['Рубрика 1', 'Рубрика 2']
results = classifier.classify_batch(my_rubrics, top_n=3)

# Экспортирую
classifier.export_results(results, 'output.csv')

# Или обрабатываю результаты дальше
for result in results:
    print(result['rubric'], result['classifications'][0]['category_name'])
```

**Плюсы:**
- Полный контроль над процессом
- Можно комбинировать с другим кодом
- Максимальная гибкость
- Идеально для автоматизации

**Минусы:**
- Нужно писать код на Python
- Требует немного знания Python

---

### Сценарий 4: "Обрабатываю огромный файл (10000+ рубрик)"

**Решение:** Батч-обработка + сохранение

```python
from rubrics_classifier import RubricsClassifier
import pandas as pd

# Загрузка
classifier = RubricsClassifier()
classifier.load_categories(categories)

# Батч-обработка (экономит память)
batch_size = 500
all_results = []

for i in range(0, len(all_rubrics), batch_size):
    batch = all_rubrics[i:i+batch_size]
    print(f"Обработка {i+1}-{min(i+batch_size, len(all_rubrics))}")
    batch_results = classifier.classify_batch(batch, top_n=3)
    all_results.extend(batch_results)

# Сохранение
classifier.export_results(all_results, 'results.csv')
```

**Время обработки:**
- 1000 рубрик: ~30 секунд
- 10000 рубрик: ~5 минут
- 100000 рубрик: ~50 минут

---

## 💻 ПРАКТИЧЕСКИЕ ПРИМЕРЫ

### Пример A: Работаю с Google Sheets

```python
# 1. Экспортирую рубрики из Google Sheets в CSV
# 2. Открываю в Excel, сохраняю как rubrics.csv
# 3. Запускаю:

python interactive_menu.py
# → Загружаю categories.csv
# → Загружаю rubrics.csv
# → Экспортирую в results.csv
# → Импортирую results.csv обратно в Google Sheets
```

---

### Пример B: Автоматизирую в батнике

**Файл `run_classification.bat` (Windows):**
```batch
@echo off
cd /d "%~dp0"

echo Классификация рубрик 2ГИС...
python quick_run.py

echo.
echo Готово! Результаты в results.csv
pause
```

**Файл `run_classification.sh` (macOS/Linux):**
```bash
#!/bin/bash
cd "$(dirname "$0")"

echo "Классификация рубрик 2ГИС..."
python quick_run.py

echo ""
echo "Готово! Результаты в results.csv"
```

---

### Пример C: Интегрирую в pandas

```python
import pandas as pd
from rubrics_classifier import RubricsClassifier

# Загрузка из Excel
df = pd.read_excel('my_2gis_data.xlsx')
rubrics_list = df['рубрика'].tolist()

# Классификация
classifier = RubricsClassifier()
classifier.load_categories(categories)
results = classifier.classify_batch(rubrics_list, top_n=1)

# Добавляю результаты в DataFrame
df['категория'] = [r['classifications'][0]['category_name'] if r['classifications'] else 'Unknown' for r in results]
df['уверенность'] = [r['classifications'][0]['confidence'] if r['classifications'] else 0 for r in results]

# Сохраняю
df.to_excel('classified_data.xlsx', index=False)
```

---

## 🎓 РЕКОМЕНДАЦИЯ ДЛЯ ТЕБЯ

**Я рекомендую НАЧАТЬ с `quick_run.py`:**

1. ✅ Простой способ понять как всё работает
2. ✅ Видишь результаты за 10 секунд
3. ✅ Без лишней сложности
4. ✅ Потом можешь перейти на интерактивное меню или свой код

---

## 🚀 ШАГИ ДЛЯ НАЧАЛА

### Шаг 1: Подготовка (5 минут)

```bash
# Создаёшь папку
mkdir rubrics_project
cd rubrics_project

# Копируешь файлы сюда:
# - rubrics_classifier.py
# - quick_run.py
# - categories.csv (из примеров)
# - rubrics.txt (из примеров)
```

### Шаг 2: Установка (2 минуты)

```bash
pip install sentence-transformers scikit-learn pandas numpy
```

### Шаг 3: Запуск (10 секунд)

```bash
python quick_run.py
```

### Шаг 4: Результаты

```
results.csv создан ✅
```

---

## ⚡ КОМАНДНАЯ СТРОКА vs ПРОГРАММА: ИТОГ

| | **Командная строка** | **Программа** |
|---|---|---|
| **Что это** | Скрипт `quick_run.py` | Интерактивное меню |
| **Как запускать** | `python quick_run.py` | `python interactive_menu.py` |
| **Ввод данных** | Файлы (categories.csv, rubrics.txt) | Интерактивный выбор файлов |
| **Просмотр результатов** | Только экспорт | Онлайн в консоли |
| **Скорость** | Быстрее (~10 сек) | Медленнее (~15 сек с меню) |
| **Удобство** | Для батча | Для разных файлов |
| **Для новичка** | ✅ Проще | ✅ Понятнее |

---

## ✅ ИТОГОВАЯ СХЕМА

```
Что нужно сделать?
    │
    ├─→ Классифицировать ОДИ большой файл?
    │   └─→ python quick_run.py ⚡ (БЫСТРО)
    │
    ├─→ Работать с РАЗНЫМИ файлами?
    │   └─→ python interactive_menu.py 🎯 (УДОБНО)
    │
    └─→ ИНТЕГРИРОВАТЬ в свой код?
        └─→ from rubrics_classifier import RubricsClassifier 🔧 (ГИБКО)
```

Всё просто! Выбери один из трёх вариантов и начни! 🚀
