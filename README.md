# Система автоматической классификации рубрик 2ГИС

Программа использует **семантические эмбеддинги** для интеллектуального распределения рубрик по категориям. В отличие от простого сопоставления слов, система понимает смысл текста и может классифицировать даже незнакомые рубрики.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install sentence-transformers scikit-learn pandas numpy openpyxl
```

**Что устанавливается:**
- `sentence-transformers` — многоязычные модели для семантического анализа
- `scikit-learn` — вычисление косинусного сходства
- `pandas` — работа с данными и экспорт
- `numpy` — численные операции
- `openpyxl` — экспорт в Excel

### 2. Запуск базового примера

```python
from rubrics_classifier import RubricsClassifier

# Определяем категории
categories = [
    {'id': 1, 'name': 'Жильё', 'description': 'Жилой комплекс; частные дома'},
    {'id': 11, 'name': 'Общепит', 'description': 'Кафе, ресторан; кофейня'},
    {'id': 13, 'name': 'Спорт', 'description': 'Спортивный клуб, тренажерный зал'},
]

# Создаем классификатор
classifier = RubricsClassifier()
classifier.load_categories(categories)

# Классифицируем рубрики
results = classifier.classify_batch(
    ['Рестораны', 'Кафе', 'Тренажерные залы'],
    top_n=3  # Вернуть топ-3 категории
)

# Экспортируем результаты
classifier.export_results(results, 'results.csv', format='csv')
```

## 🔧 Использование

### Загрузка категорий из разных источников

#### Вариант 1: Из словаря (в коде)
```python
categories = [
    {'id': 1, 'name': 'Жильё', 'description': 'Описание...'},
    {'id': 2, 'name': 'Общепит', 'description': 'Описание...'},
]
classifier.load_categories(categories)
```

#### Вариант 2: Из CSV файла
```python
import csv

def load_from_csv(path):
    categories = []
    with open(path, encoding='utf-8') as f:
        reader = csv.DictReader(f)
        for row in reader:
            categories.append({
                'id': int(row['№']),
                'name': row['Тип'],
                'description': row['Общее описание']
            })
    return categories

categories = load_from_csv('categories.csv')
classifier.load_categories(categories)
```

#### Вариант 3: Из JSON файла
```python
import json

with open('categories.json', encoding='utf-8') as f:
    categories = json.load(f)

classifier.load_categories(categories)
```

### Классификация

#### Одна рубрика
```python
# Возвращает список (category_id, category_name, confidence_score)
results = classifier.classify_rubric(
    'Кинотеатры',
    top_n=3,
    threshold=0.0  # Минимальный скор (0-1)
)

for cat_id, cat_name, confidence in results:
    print(f"{cat_name}: {confidence:.2%}")
```

#### Несколько рубрик (батч)
```python
rubrics = ['Рестораны', 'Больницы', 'Тренажерные залы']

results = classifier.classify_batch(
    rubrics,
    top_n=3,  # Топ-3 категории для каждой рубрики
    threshold=0.0  # Отфильтровать по минимальному уверенности
)

for result in results:
    print(f"\nРубрика: {result['rubric']}")
    for clf in result['classifications']:
        print(f"  → {clf['category_name']} ({clf['confidence']:.1%})")
```

### Экспорт результатов

```python
# В CSV (самый популярный формат)
classifier.export_results(results, 'output.csv', format='csv')

# В JSON
classifier.export_results(results, 'output.json', format='json')

# В Excel
classifier.export_results(results, 'output.xlsx', format='xlsx')
```

## 📊 Формат результатов

### JSON формат
```json
[
  {
    "rubric": "Рестораны",
    "classifications": [
      {
        "category_id": 11,
        "category_name": "Общепит",
        "confidence": 0.9534
      },
      {
        "category_id": 26,
        "category_name": "Крупный торговый центр",
        "confidence": 0.1234
      }
    ]
  }
]
```

### CSV формат
```
Рубрика,Категория_1,Уверенность_1,Категория_2,Уверенность_2,Категория_3,Уверенность_3
Рестораны,Общепит,0.9534,Крупный торговый центр,0.1234,Торговля,0.0821
```

## 🧠 Как это работает

### Алгоритм

1. **Текст → Вектор** — система преобразует текст рубрики и категорий в многомерные вектора (эмбеддинги)
2. **Сравнение** — вычисляет косинусное сходство между вектором рубрики и всеми векторами категорий
3. **Ранжирование** — сортирует категории по степени сходства
4. **Результат** — возвращает топ-N категорий с оценками вероятности (0-1)

### Преимущества перед простым поиском слов

✅ **Понимает синонимы** — "кафе" и "кофейня" будут классифицированы одинаково  
✅ **Игнорирует опечатки** — близкие опечатки будут распознаны  
✅ **Контекстное понимание** — учитывает общее описание категории  
✅ **Работает с незнакомыми рубриками** — даже если рубрика не встречалась в тренировочных данных  

## 🎯 Рекомендации

### Выбор threshold (порога уверенности)

```python
# Только высокоуверенные результаты (может пропустить 10-20%)
results = classifier.classify_batch(rubrics, threshold=0.7)

# Все результаты
results = classifier.classify_batch(rubrics, threshold=0.0)
```

### Для больших объемов данных

```python
# Обработка батчами (экономит память)
batch_size = 100
all_results = []

for i in range(0, len(all_rubrics), batch_size):
    batch = all_rubrics[i:i+batch_size]
    batch_results = classifier.classify_batch(batch, top_n=3)
    all_results.extend(batch_results)
```

### Анализ качества

```python
from examples_usage import validate_classification_quality, filter_results_by_confidence

# Статистика
quality = validate_classification_quality(results, min_confidence=0.6)
print(f"Уверенных классификаций: {quality['confidence_rate']}%")
print(f"Требуют проверки: {quality['unclassified_rate']}%")

# Разделить на уверенные и сомнительные
filtered = filter_results_by_confidence(results, min_confidence=0.6)
confident = filtered['confident']  # Готовые к использованию
uncertain = filtered['uncertain']  # Требуют ручной проверки
```

## 📝 Примеры использования

### Пример 1: Классификация рубрик 2ГИС

```python
# Загружаем список рубрик из 2ГИС
from examples_usage import load_rubrics_from_file

rubrics = load_rubrics_from_file('2gis_rubrics.csv')

# Классифицируем все
classifier = RubricsClassifier()
classifier.load_categories(categories)
results = classifier.classify_batch(rubrics, top_n=3)

# Экспортируем
classifier.export_results(results, 'classified_rubrics.csv')
```

### Пример 2: Обработка с разделением результатов

```python
from examples_usage import export_results_with_summary

# Это сохранит:
# - all_classifications.csv (все результаты)
# - confident_classifications.csv (уверенные результаты)
# - uncertain_classifications.csv (требуют проверки)
# - quality_report.json (статистика)

export_results_with_summary(
    results,
    output_dir='classification_output',
    min_confidence=0.6
)
```

## 🔍 Отладка

### Проверить качество на конкретной рубрике

```python
# Смотрим все варианты и их оценки
results = classifier.classify_rubric('Название рубрики', top_n=10)

for cat_id, cat_name, score in results:
    bar = '█' * int(score * 20)
    print(f"[{bar:<20}] {cat_name:40} {score:.1%}")
```

### Улучшить классификацию

Если результаты неточные, можно:

1. **Улучшить описание категории** — добавить ключевые слова в "Общее описание"
   ```python
   {'id': 11, 'name': 'Общепит', 'description': 'Кафе, ресторан, кофейня, столовая, пекарня, закусочная, столовая'}
   ```

2. **Использовать другую модель** — есть более точные, но медленные модели
   ```python
   classifier = RubricsClassifier(
       model_name="sentence-transformers/distiluse-base-multilingual-cased-v2"
   )
   ```

3. **Настроить top_n и threshold** — искать оптимальные параметры для ваших данных

## ⚙️ Технические детали

### Используемая модель

По умолчанию: `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`
- ✅ Быстрая (работает на CPU)
- ✅ Компактная (110M параметров)
- ✅ Поддерживает 50+ языков
- ✅ Хорошее качество для классификации

Альтернативы:
- `sentence-transformers/distiluse-base-multilingual-cased-v2` — более точная, медленнее
- `sentence-transformers/LaBSE` — для многоязычного текста
- `sentence-transformers/all-mpnet-base-v2` — очень точная, требует больше ресурсов

### Требования

- Python 3.7+
- ~2GB RAM для моделей
- Интернет для первой загрузки модели (она кэшируется локально)

## 📞 Типичные вопросы

**Q: Почему классификация неправильная?**  
A: Проверьте описание категорий (Общее описание) — оно должно содержать ключевые примеры. Также можно увеличить top_n для более мягкой классификации.

**Q: Можно ли работать без интернета?**  
A: Да, модель загружается один раз при первом запуске и кэшируется локально.

**Q: Как ускорить обработку больших объемов?**  
A: Используйте batch-обработку и GPU (если доступна), установив torch с CUDA поддержкой.

**Q: Поддерживает ли английский и другие языки?**  
A: Да, модель работает с 50+ языками одновременно.

## 📄 Лицензия

MIT License — используйте свободно в своих проектах.
