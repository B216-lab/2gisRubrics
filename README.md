# Classification System Pro

Полнофункциональная система классификации фирм по категориям рубрик с поддержкой 2GIS формата данных, ручного обучения модели и коррекции результатов.

## 🚀 Быстрый старт

### 1. Установка

```bash
# Клонируйте репозиторий или распакуйте архив
cd classification_system

# Установите зависимости
pip install -r requirements.txt
```

### 2. Подготовка данных

Поместите файл CSV из 2GIS в директорию `data/`:
- Должен быть файл с колонками: `Наименование`, `Описание`, `Рубрики`, `Адрес`, `Тип`
- Кодировка: UTF-8

Пример:
```
data/
├── companies.csv          # Ваш файл из 2GIS
├── categories.csv         # Справочник категорий
└── training_rules.json    # Правила обучения (создается автоматически)
```

### 3. Запуск

#### Интерактивный режим (рекомендуется):
```bash
python main.py
```

#### Командная строка:
```bash
# Классифицировать файл
python main.py --input data/companies.csv --output output/result.csv

# Добавить правило обучения
python main.py --add-rule "ЗАГС" "ЗАГС" --priority 90

# Показать все правила
python main.py --show-rules

# Генерировать отчет
python main.py --input data/companies.csv --report output/report.json
```

## 📋 Структура проекта

```
classification_system/
├── main.py                 # Главный файл, точка входа
├── config.py              # Конфигурация
├── classifier.py          # Логика классификации (2 уровня)
├── data_processor.py      # Обработка CSV данных
├── ui.py                  # CLI интерфейс
├── requirements.txt       # Зависимости Python
├── README.md             # Этот файл
│
├── data/
│   ├── companies.csv      # Входные данные из 2GIS
│   ├── categories.csv     # Справочник 48 категорий
│   └── training_rules.json # Правила обучения (создается автоматически)
│
├── models/
│   ├── classifier_model.pkl   # Кэшированная модель
│   ├── vectorizer.pkl         # TF-IDF векторизатор
│   └── label_encoder.pkl      # Кодировщик меток
│
└── output/
    ├── classified_companies.csv   # Результаты классификации
    ├── report.json               # Статистический отчет
    └── corrections.json          # История исправлений
```

## 🎯 Основные функции

### 1. 📂 Загрузка данных
- Поддерживает CSV формат из 2GIS (parser-2gis)
- Автоматическое определение кодировки
- Валидация структуры данных

### 2. 🔄 Двухуровневая классификация
**Уровень 1**: Классификация по рубрикам (метаинформация)
**Уровень 2**: Классификация по названию и описанию фирмы

Система объединяет оба результата для максимальной точности.

### 3. 📚 Ручное обучение модели
Добавьте правила для исправления ошибок:

```bash
# Правило: если в тексте есть "ЗАГС", классифицировать как "ЗАГС"
python main.py --add-rule "ЗАГС" "ЗАГС" --priority 90
python main.py --add-rule "регистрация" "ЗАГС" --priority 80
python main.py --add-rule "свидетельство о рождении" "ЗАГС" --priority 85
```

Правила с высоким приоритетом переопределяют модель.

### 4. ✓ Проверка и корректировка
- Просмотр компаний с низкой уверенностью (< 60%)
- Быстрая корректировка через интерфейс
- История всех исправлений в `output/corrections.json`

### 5. 📊 Статистика и отчеты
- Распределение по категориям
- Средняя уверенность классификации
- Количество проблемных классификаций
- Полный JSON отчет

### 6. 📥 Экспорт результатов
- CSV с классифицированными данными
- CSV с оригинальными данными + классификация
- JSON отчеты

## 💡 Рабочий процесс

### Первый запуск:
```
1. Запустите: python main.py
2. Выберите "1. Загрузить компании"
3. Укажите путь: data/companies.csv
4. Выберите "2. Классифицировать"
5. Дождитесь завершения
```

### Улучшение качества:
```
1. Выберите "3. Генерировать отчет" (увидите статистику)
2. Выберите "5. Проверка и корректировка"
3. Исправляйте ошибочные классификации
4. Система автоматически добавляет правила обучения
5. Повторите классификацию для финальной версии
```

### Пакетная обработка:
```bash
# Обработать все в одной команде
python main.py \
  --input data/companies.csv \
  --output output/classified.csv \
  --report output/report.json
```

## 🔧 Расширенные возможности

### Добавление собственных категорий
Отредактируйте `data/categories.csv`:
```csv
№;Тип;Описание
1;Жильё;Жилые комплексы и частные дома
...
```

### Настройка параметров модели
Отредактируйте `config.py`:
```python
class ClassifierConfig:
    min_confidence_threshold = 0.5
    priority_boost = 100
    vectorizer_max_features = 5000
    vectorizer_ngram_range = (1, 2)
```

### Интеграция в другие программы
```python
from classifier import CompanyClassifier

classifier = CompanyClassifier()

company_data = {
    'name': 'ООО ЗАГС-24',
    'description': 'Помощь в регистрации актов гражданского состояния',
    'rubrics': 'ЗАГС, Юридические услуги',
    'address': 'ул. Пушкина, 10'
}

result = classifier.classify_company(company_data)
print(result)
# {
#   'final_category': 'ЗАГС',
#   'final_confidence': 0.92,
#   'top_3': [('ЗАГС', 0.92), ('Юридические услуги', 0.68), ...]
# }
```

## 📊 Формат результатов

### CSV выходной файл
```csv
company_name,level1_category,level1_confidence,level2_category,level2_confidence,final_category,final_confidence
ЛЮКС МОНТАЖ,Строящиеся здания,0.78,Торговля (мебель),0.81,Торговля (мебель),0.81
Первый Ленинский квартал,Жильё,0.89,Жильё,0.91,Жильё,0.91
...
```

### JSON отчет
```json
{
  "total_companies": 1500,
  "unique_categories": 45,
  "avg_confidence": 0.75,
  "low_confidence_items": 120,
  "categories_distribution": {
    "Жильё": 250,
    "ЗАГС": 45,
    "Отдел полиции": 38,
    ...
  }
}
```

## 🐛 Решение проблем

### Проблема: Недостаточно точность
**Решение**: Добавьте правила обучения для проблемных категорий
```bash
python main.py --add-rule "ключевое_слово" "категория" --priority 90
```

### Проблема: Модель не загружается
**Решение**: Удалите кэш и переобучите
```bash
rm models/*.pkl
# Или выберите в интерфейсе "Не использовать кэш"
```

### Проблема: CSV не загружается
**Решение**: Проверьте:
- Кодировка файла (должна быть UTF-8)
- Наличие обязательных колонок
- Отсутствие спецсимволов в начале файла

## 📈 Производительность

- **Загрузка**: ~1000 компаний в секунду
- **Классификация**: ~100 компаний в секунду (зависит от GPU)
- **Память**: ~500 МБ для модели + 1-2 МБ на каждые 1000 компаний

## 🎓 Техническая информация

### Используемые алгоритмы
- **TF-IDF**: Для представления текста
- **Random Forest**: Классификация с 200 деревьями
- **Двухуровневая архитектура**: Для максимальной точности

### Поддерживаемые языки
- Русский ✓
- Английский (частично)

## 📝 Лицензия

MIT License - используйте свободно

## 👨‍💻 Автор

Система создана для автоматической классификации рубрик из 2GIS с фокусом на практическое применение и удобство использования.

## 🤝 Обратная связь

Если у вас есть предложения по улучшению, найдены баги или вы хотите добавить функции - свяжитесь через issues/discussions.

---

**Версия**: 1.0  
**Последнее обновление**: 2026-01-09  
**Python**: 3.8+
